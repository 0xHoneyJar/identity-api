{
  "name": "@freeside-auth/adapters",
  "version": "0.1.0",
  "private": true,
  "description": "Typed clients implementing ports — PostgresSplitAdapter (mibera) · PostgresUnifiedAdapter (cubquest) · JWKS validator (cache + stale-if-error) · credential bridges (Dynamic / Better Auth / SeedVault → canonical proof). Wire-side, not pure logic.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./postgres-split": "./src/postgres-split-adapter.ts",
    "./http-inventory": "./src/http-inventory-adapter.ts",
    "./http-score": "./src/http-score-adapter.ts",
    "./http-codex": "./src/http-codex-adapter.ts",
    "./federation-http": "./src/federation-http.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@effect/schema": "^0.75",
    "@freeside-auth/engine": "workspace:*",
    "@freeside-auth/ports": "workspace:*",
    "@freeside-auth/protocol": "workspace:*",
    "effect": "^3.10.0",
    "jose": "^6.2.3"
  },
  "peerDependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
